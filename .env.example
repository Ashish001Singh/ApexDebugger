# Copy to .env — never commit .env
ANTHROPIC_API_KEY=sk-ant-...

# Phase 3 — Supabase (leave blank for Phase 1-2)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Phase 3 — Voyage embeddings (optional, can use Claude instead)
VOYAGE_API_KEY=

# App settings
LOG_LEVEL=INFO
ENVIRONMENT=development
